+ 1. Вывод одного выражения таблицы умножения.
	+ Создаем в папке Model класс Expression, екземпляр которого будем создавать в контроллере.
	+ Создаем в классе: Свойство А для первого числа и свойство В для второго числа, метод Result который будет умножать числа.
	+ В контролерре создаем ActionResult и View для него.
	+ В  ActionResult создаем екземпляр класса Expression, и задаем значения чисел.
	+ Во ViewBag вкладываем созданный екземпляр.
	+ Переходим во View.Через Html хелпер (@Html.Partial) пишем Partial View, аргументы - название представления (создадим в следующем пункте)
		и объект который создали в ActionResult.
		Пример: @Html.Partial("Expression", (Expression)ViewBag.Expression) 
	+ Partial View создаем через решарпер (alt+enter) на названии представления в @Html.Partial.
	+ В созданном представлении выводим строку, которая будет состоять из переданых через во ViewBag свойств и метода.
+ 2. Вывести отдельно примеры отдельной таблицы (например на 2)
+ 3. Сформировать представление для всей таблицы	